.card.review-post-form
  button.card-header.button
    a レビューを投稿する
  div class = 'card-body target' target = "_blank"
    = form_with model: [@book, @review], local: true do |f|
      = render 'shared/error_messages', object: f.object

      .form-group
        = f.label :title, 'タイトル'
        = f.text_field :title, class: 'form-control'
      .form-group
        = f.label :content, 'レビュー'
        a.must-mark.bg-danger 必須
        a.spoiler-check ※ネタバレあり
        = f.check_box :spoiler, id: 'spoiler-check'
        = f.text_area :content, class: 'form-control', id: 'review-content'
    
        = button_tag type: 'submit', class: 'form-control btn btn-primary', id: 'review-post-btn' do
          i.far.fa-file-alt
          span 投稿
  